import React from 'react'
import Head from '../Head';
import Search from '../Search';

const Main: React.FC = () => {
    return (
        <div>
            <Head></Head>
            <Search></Search>
        </div>
    );
};

export default Main
